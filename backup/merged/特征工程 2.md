# 特征工程

痛点:

- 一个场景和一个模型还好，很多场景很多模型，还需要变更
- 正确性、
- 复用、

生产：

- pipeline管理，血缘、监控
- 实时和离线特征最终都要到kv存储里面去
- 生产和使用一致性

使用：

- 性能
- 正确
- 效率：方便，特征的转换

管理：

- 上下线
- 使用监控：稳定性

通过lib包落地全局性的设计：schema、类型、关联

通过平台管理系统管理起来

规模化之后自动起来： 自动的数据

**特征存储为中介，**

→ 导出到kv（redis、持久化的kv），本地缓存

→ 格式转换

→ 访问的统计

## 生产、使用、管理

生产： source

- 全站、系统内的log

实时和离线

实时特征：当前用户的访问、点击行为、搜索词

## 1、管理、监控、数据质量自动化、平台级别的复用

2、 特征的上下线， 新增特征之后

3、

实时特征：

- 特征快照log，放入kafka
- chanel

![Image.tiff](%E7%89%B9%E5%BE%81%E5%B7%A5%E7%A8%8B%202.assets/Image.tiff)

特征的质量、数据分布

联动起来： 一致性

- 让离在线都用同一个lib
- 在框架层提供了统一的DAL层， loadkey关联模型，特征配置。
- 特征的转换、使用、

## 数据，具体来说是特征

- 离线训练

![Image (2).tiff](%E7%89%B9%E5%BE%81%E5%B7%A5%E7%A8%8B%202.assets/Image%20(2).tiff)

一、管理、监控、数据质量自动化、平台级别的复用

如果特征难以访问，以下情况会发生：

- 不同团队反复实现同一个特征，导致同一特征可能有多达 10 个版本。
- 部署多个相近的特征管道，浪费计算和存储资源。
- 因为同一特征有多个版本，不同模型会使用不同版本的特征，很难得到一致的结果。
- 迭代变慢。

在这个层次上，特征平台基本上是个包含很多特征的存储，和数据仓库的区别不大。

最底层是访问（access）的需求。平台化的管理起来

- 这一层需求包括**特征可读取、特征转换逻辑透明和特征血缘可溯**。它们使得特征**能被发现、分享和复用，减少重复。**

最后是自治（autopilot）的需求。

- 包括**自动回填特征、对特征的分布进行监控和报警等**。

便利的需求。

- 特征平台需要足够简单好入手，例如提供简单直观的接口、易交互、易 debug 等，才能让大家采纳和受益。

特征的修改与版本

二、线上服务

其次是服务（serving）的需求。

- 这一层的核心需求是**为线上服务提供高吞吐、低延迟的特征读取**能力，而无需通过 SQL 去数据仓库读取。
- 其它需求还包括：与已有的离线特征存储集成，使得特征能够**从离线特征存储同步到在线特征存储（例如 Redis）；实时的特征转换等。**
- 访问、

提供SDK、schema，    封装访问地址，一致的API

离线同步处理工具。

实时特征

非深度学习的模型，特征工程更加重要。

三、准确（integrity）需求。

- 最常见的需求是最小化 train-serve skew，确保特征在**训练和服务环境下是一致的**。
- 另一个常见需求是 point-in-time correctness（又称 time-travel），以确保**历史特征和标签被用于训练和评估时不存在 data leaks。**

本质上是一个大数据处理系统