# 架构

### 如何讲解架构

- 言简意赅
- 首先讲解本质，再分为功能架构和数据架构

围绕着模型、数据、逻辑；

搜索是围绕着索引，算分做文章。

[脚踏实地：来自Google的推荐算法实践](https://mp.weixin.qq.com/s/8AjXF2jWHZgpNJA3yCEopg)

1、在线框架 - 算子化

2、九州系统 - 保驾护航

3、数据流动

## 工程问题收集

- TDM实战遇到的问题
    - 也是工程上最大难点
- 向量化召回是主流
    - 离线算好和在线检索的优缺点
        - 需要更新，kv存储面临2倍存储。切换的问题

[阿里定向广告最新突破：面向下一代的粗排排序系统COLD](https://zhuanlan.zhihu.com/p/186320100)

- 工程优化是重点。             能够实时在线训练
    - 特征计算部分主要负责从索引中拉取用户和item的特征并且进行交叉特征的相关计算。
    - 而网络计算部分，会将特征转成embedding向量，并将它们拼接进行网络计算

以上假设对于feature embedding是成立的，但是模型中DNN各层权重可怎么办？worker要完成训练，必须要拿到完整的、所有各层dnn weight不可。

### 每个阶段得亮点

- 架构核心要解决的问题
    - 大数据量下的性能
    - 实时更新，模型的更新
    - 不同算法下的工程挑战
    - 多场景、多算法人员
        - 并行发布问题
        - 复用
        - 不干扰问题
- 如何实现，分为哪些模块
    - 只需要填写code；
    - 流量分发，赛马架构；
    - 公司级的复用和小组级别的复用
    - 一般的解法，我们得解法
        - 阿里架构，我们的
        - 问题的本质，相同点，不同点。
    - 数据架构
        - 特征join后的在线训练
        - log、特征增加减少，无须
    - TDM、SIM算法中的工程难题
        - 向量检索
        - 离线索引以及更新
    - 参数并行架构和deepspeed千亿并行架构区别

Debug

- 一个trace，走了哪些算子
- 用户白板
- 分类看badcase

工具&框架

- deepspeed的N种并行方式  与  ray的差别。
- mxnet、pytorch等框架的差异
- 向量数据库

性能问题

- 模型压缩和蒸馏的本质
- 伯努利架构

## 理解网络结构和数学表示

- [https://zhuanlan.zhihu.com/p/34940250](https://zhuanlan.zhihu.com/p/34940250)
- [https://github.com/Snail110/recsys](https://github.com/Snail110/recsys)
- 神经网络是一种数据结构；表示学习，loss
- pytorch或者TensorFlow表示这种结构

从工程角度看算法落地的痛点：

[三问TDM](https://mp.weixin.qq.com/s/VFXLwZxKbmbJUYpSAPdSHg)

[阿里深度树匹配召回体系演进](https://zhuanlan.zhihu.com/p/417643436)

TDM vs PDM

- 如何在匹配阶段的计算效率约束下引入更先进的复杂深度学习模型成为了下一代匹配技术发展的重要方向。
    - 高阶深度学习大部分都不可划成内积形式，比如CTR预估里用户和商品特征交叉非常有用，大部分不可用内积表示；
    - 以向量距离的方式衡量用户和商品兴趣度的方法，用户和商品被表示成向量形式，并以此为基础建立基于向量聚类的索引结构进一步加速衡量效率，于是这个计算问题变成了在有限时间内是可解的（近似求解），具体实现也落到了向量引擎的范畴；
- 匹配技术发展的核心点在于系统性能限制下尽可能提升兴趣的衡量精度（从规则算法到引入先进模型）和覆盖范围（从限定候选集到全量候选集）。

概念上万物皆通，真正难的是否能在数学和技术上构建出一条真正可行的路径。就匹配问题而言，树检索结构的引入伴随着一系列的问题要解决：

1、树结构是如何构建的；

2，如何基于树进行匹配建模；

3、如何围绕树结构实现高效的兴趣计算和检索。

理解算法后，能够1+1 > 2 ,解法算法同学的idea。 大公司公布了新的算法，但是受限于工程能力，不好落地。 比如

PDM依旧是向量化内积召回

- 如何表示建模这个内积
- 向量模型和精排模型联合训练：
    - 向量模型和精排模型共享部分embedding，同时向量模型仅在展现样本上以交叉熵loss进行训练，精排模型也仅在展现样本上以交叉熵loss进行训练。
- batch内随机负采样：为了提升模型对简单样本的区分能力，缓解SSB问题，这里引入随机负样本。为了减少实现成本，这里给每条点击正样本在batch内随机选k个ad向量，和这条正样本的user向量拼在一起组成随机负样本。为了避免随机负样本影响pctr预估精度，这里在原有双塔网络基础上额外构建了一个双塔网络，新双塔网络和原双塔网络的前几层参数共享，输出为aux_pctr，aux_pctr基于正样本和随机负样本以交叉熵loss参与训练。
- unpv样本上的distill：
    - 这里将精排未展现样本以distill方式用精排rank_pctr对向量模型训练进行指导，通过这种方式来提升召回模型和精排模型在召回空间上的打分一致性，从而缓解召回阶段的SSB问题，同时训练过程中要通过stop_gradient的方式来屏蔽蒸馏loss对精排训练的影响：

[阿里广告技术最新突破：全链路联动-面向最终目标的全链路一致性建模](https://zhuanlan.zhihu.com/p/413240790)

[推荐召回算法之深度树模型](https://zhuanlan.zhihu.com/p/585750630)

[召回--联合训练树的索引和深层模型在推荐系统中的应用_frank_hetest的博客-CSDN博客](https://blog.csdn.net/hestendelin/article/details/120278835)